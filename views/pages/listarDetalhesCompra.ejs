<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
    <script type="text/javascript">

    </script>
</head>
<body class="container">

    <header>
        <% include ../partials/header %>
    </header>

    <main>
        <div class="jumbotron">
          <% valorCompra = 0.00; %>
          <h3>Cod Compra: <%= data[0].idCompra%> // Fornecedor: <%=data[0].fornecedorCompra%> // Data: <%=data[0].dataCompra%></h3><br>
          <h3>Produtos</h3>
          <table class="table table-striped">
            <h4>Lista de Produtos</h4>
            <tr>
              <th>Cod. Produto</th><th>Quantidade</th><th>Marca</th><th>Descrição</th><th>Valor Unitário</th><th>Ação</th>
            </tr>
          <%for(var i = 0; i < data.length ;i++){
            valorCompra += data[i].qtdCompra * data[i].valorUnitario%>
            <tr>
              <td><%= data[i].idProduto%></td>
              <td><%= data[i].qtdCompra%></td>
              <td><%= data[i].marcaProduto%></td>
              <td><%= data[i].descProduto%></td>
              <td>R$ <%= data[i].valorUnitario%></td>
              <td><a class="btn btn-info" >Detalhes</a></td>
            </tr>

          <%}%>
          </table>
          <p>Valor Total: R$ <%= valorCompra %></p>
  			</div>

    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>

</body>
</html>
